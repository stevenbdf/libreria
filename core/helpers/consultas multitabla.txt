

Select precioVenta, cantidad, ISBN, fecha from detallepedido inner join pedidos on pedidos.idPedido=detallepedido.idPedido;
select hora, fecha, nombreCliente, apellidoCliente, correo from comentnoticias inner join cliente on comentnoticias.idClient=cliente.idCliente;
select fecha, titulo, nombreEmpleado from noticias inner join empleados on noticias.idEmpleado=empleados.idEmpleado;

DELIMITER |

CREATE TRIGGER test AFTER INSERT ON libros
  FOR EACH ROW BEGIN
  DECLARE @fecha datetime
  SET @fecha=(SELECT getdate());
    INSERT INTO bitacora(1, @fecha, 'Insertó un libro'
  END
|

DELIMITER ;
